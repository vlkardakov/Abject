"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.VoiceActivation = void 0;
const PacketUtil_1 = __importDefault(require("../PacketUtil"));
const CodecInfo_1 = require("./CodecInfo");
var VoiceActivation;
(function (VoiceActivation) {
    function deserialize(packet) {
        const name = packet.readUTF();
        const translation = packet.readUTF();
        const icon = packet.readUTF();
        const distances = PacketUtil_1.default.readIntList(packet);
        const defaultDistance = packet.readInt();
        const proximity = packet.readBoolean();
        const transitive = packet.readBoolean();
        const stereoSupported = packet.readBoolean();
        const hasEncoderInfo = packet.readBoolean();
        let encoderInfo;
        if (hasEncoderInfo) {
            encoderInfo = CodecInfo_1.CodecInfo.deserialize(packet);
        }
        const weight = packet.readInt();
        return {
            name,
            translation,
            icon,
            distances,
            defaultDistance,
            proximity,
            transitive,
            stereoSupported,
            hasEncoderInfo,
            encoderInfo: hasEncoderInfo ? encoderInfo : undefined,
            weight,
        };
    }
    VoiceActivation.deserialize = deserialize;
})(VoiceActivation || (exports.VoiceActivation = VoiceActivation = {}));
