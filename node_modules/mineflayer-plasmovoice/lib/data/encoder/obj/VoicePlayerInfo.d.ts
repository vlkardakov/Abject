import ByteArrayDataInput from "../ByteArrayDataInput";
export type VoicePlayerInfoData = {
    playerId: UUID;
    playerNick: string;
    muted: boolean;
    voiceDisabled: boolean;
    microphoneMuted: boolean;
};
export declare namespace VoicePlayerInfo {
    function deserialize(packet: ByteArrayDataInput): VoicePlayerInfoData;
}
