"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EncryptionInfo = void 0;
var EncryptionInfo;
(function (EncryptionInfo) {
    function deserialize(packet) {
        const algorithm = packet.readUTF();
        const length = packet.readInt();
        const data = new Uint8Array(length);
        packet.readFully(data);
        return {
            algorithm,
            data,
        };
    }
    EncryptionInfo.deserialize = deserialize;
})(EncryptionInfo || (exports.EncryptionInfo = EncryptionInfo = {}));
