"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CaptureInfo = void 0;
const CodecInfo_1 = require("./CodecInfo");
var CaptureInfo;
(function (CaptureInfo) {
    function deserialize(packet) {
        // Capture Info
        const sampleRate = packet.readInt();
        const mtuSize = packet.readInt();
        const hasEncoderInfo = packet.readBoolean();
        let encoderInfo;
        if (hasEncoderInfo) {
            encoderInfo = CodecInfo_1.CodecInfo.deserialize(packet);
        }
        return {
            sampleRate,
            mtuSize,
            hasEncoderInfo,
            encoderInfo: hasEncoderInfo ? encoderInfo : undefined,
        };
    }
    CaptureInfo.deserialize = deserialize;
})(CaptureInfo || (exports.CaptureInfo = CaptureInfo = {}));
