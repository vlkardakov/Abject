"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CodecInfo = void 0;
var CodecInfo;
(function (CodecInfo) {
    function deserialize(packet) {
        const name = packet.readUTF();
        const params = new Map();
        const size = packet.readInt();
        for (let i = 0; i < size; i++) {
            params.set(packet.readUTF(), packet.readUTF());
        }
        return {
            name,
            params,
        };
    }
    CodecInfo.deserialize = deserialize;
})(CodecInfo || (exports.CodecInfo = CodecInfo = {}));
