"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.McGameProfileSerializer = void 0;
const PacketUtil_1 = __importDefault(require("../PacketUtil"));
var McGameProfileSerializer;
(function (McGameProfileSerializer) {
    function deserialize(packet) {
        const id = PacketUtil_1.default.readUUID(packet);
        const name = packet.readUTF();
        const length = packet.readInt();
        const properties = new Set();
        for (let k = 0; k < length; k++) {
            properties.add([
                packet.readUTF(),
                packet.readUTF(),
                packet.readUTF(),
            ]);
        }
        return {
            id,
            name,
            properties,
        };
    }
    McGameProfileSerializer.deserialize = deserialize;
})(McGameProfileSerializer || (exports.McGameProfileSerializer = McGameProfileSerializer = {}));
