"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class PacketUtil {
    static readUUID(packet) {
        return {
            mostSignificantBits: packet.readLong(),
            leastSignificantBits: packet.readLong(),
        };
    }
    static writeUUID(packet, uuid) {
        packet.writeLong(uuid.mostSignificantBits);
        packet.writeLong(uuid.leastSignificantBits);
        return packet;
    }
    static readIntList(packet) {
        const size = packet.readInt();
        const list = [];
        for (let i = 0; i < size; i++)
            list.push(packet.readInt());
        return list;
    }
}
exports.default = PacketUtil;
