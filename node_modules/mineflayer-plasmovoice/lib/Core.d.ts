import { Bot } from "mineflayer";
import PacketClientHandler from "./packet/PacketClientHandler";
import PacketSocketHandler from "./packet/PacketSocketHandler";
import StoredData from "./packet/StoredData";
export default class Core {
    readonly bot: Bot;
    readonly packetClientHandler: PacketClientHandler;
    readonly packetSocketHandler: PacketSocketHandler;
    readonly storedData: StoredData;
    private lastPlayerAudioPacketTimestamp;
    private stopFlag;
    constructor(bot: Bot);
    isTalking(): boolean;
    stopTalking(): Promise<void>;
    sendPCM(pcmBuffer: Buffer, distance: number, activation: string): Promise<void>;
}
